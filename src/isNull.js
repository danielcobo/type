/**
 * Returns true/false if input is null
 * @param {*} val - input value
 * @returns {boolean} true/false input value is null
 */
const isNull = function isNull(val) {
  return val === null;
};

module.exports = isNull;
